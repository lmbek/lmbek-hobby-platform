apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: platform-tls
spec:
  secretName: staging-platform-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - staging.lmbek.dk